using UnityEngine;

namespace GameObjects.BoardSpaces.CardSpaces {
    public class ChanceSpace: DrawCardTile
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }


        public ChanceSpace(bool chanceOrCommunityChest) : base(chanceOrCommunityChest){
        }
    }
}
