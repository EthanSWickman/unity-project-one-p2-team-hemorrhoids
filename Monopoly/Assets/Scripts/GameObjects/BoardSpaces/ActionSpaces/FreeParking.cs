using UnityEngine;

namespace GameObjects.BoardSpaces.ActionSpaces {
    public class FreeParling : Space
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
